{
    "name": "snaky-arena-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "frontend": "npm run dev --prefix frontend",
        "backend": "cd backend && uv run uvicorn app.main:app --host 0.0.0.0 --port 3000 --reload",
        "dev": "concurrently -n \"FRONT,BACK\" -c \"bgBlue.bold,bgMagenta.bold\" \"npm run frontend\" \"npm run backend\""
    },
    "devDependencies": {
        "concurrently": "^9.1.0"
    }
}