# Database Configuration

# SQLite (default for development and testing)
# DATABASE_URL=sqlite:///./snaky_arena.db

# PostgreSQL (for production)
# DATABASE_URL=postgresql://username:password@localhost:5432/snaky_arena

# Example with cloud PostgreSQL (e.g., Heroku, Render)
# DATABASE_URL=postgresql://user:pass@host.example.com:5432/dbname

# JWT Secret Key (change in production!)
# SECRET_KEY=your-secret-key-here
